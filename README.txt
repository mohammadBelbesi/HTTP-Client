•mohammad belbesi
•HTTP client
•client.c : our program "client"
•in this ex we implement an HTTP client that constructs an HTTP request based on the user’s command line input,
 sends the request to a Web server, receives the reply from the server, and displays the reply message on the screen.
